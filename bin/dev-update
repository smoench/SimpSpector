#!/usr/bin/env bash

chmod -Rf ug+w var
composer install --dev
bin/console doctrine:migrations:migrate -n -q

cd assets
bower install
npm install
gulp styles
gulp js

cd ../web/css
ln -s ../../assets/bower_components/semantic/dist/themes